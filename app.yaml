# https://cloud.google.com/appengine/docs/the-appengine-environments#comparing_high-level_features
# https://cloud.google.com/appengine/docs/standard

runtime: java17
env: flex                    # switch to Flexible environment

resources:
  cpu: 1
  memory_gb: 0.5
  disk_size_gb: 10

network:
  session_affinity: true     # needed for websockets

env_variables:
  GOOGLE_CLIENT_ID: ""
  GOOGLE_CLIENT_SECRET: ""
  GOOGLE_REDIRECT_URI: "https://grounded-jetty-490810-t0.oa.r.appspot.com/api/calendar/callback"
  FRONTEND_REDIRECT_URI: "https://sopra-fs26-group-08-client.vercel.app/"